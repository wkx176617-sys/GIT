# 更新记录

## v1.7.6 - 2026-08-13

- GitHub首页改为奶油白、雾粉和鼠尾草绿的轻盈编辑风格，新增原创安全连接横幅。
- 徽章统一为低饱和配色，减少工程仪表盘感，保留版本、检查和兼容信息。
- 首页结构改为开始搭建、日常使用、可选插件和遇到问题四个主要入口。
- 技术模块和维护资料改用折叠区，降低首次浏览的信息密度但不删除内容。
- 新增简短视觉与文案规范，防止后续更新重新堆成工程控制台。
- 横幅压缩为约124KB，不引入脚本、字体、外链追踪或运行性能负担。

## v1.7.5 - 2026-08-13

- GitHub首页新增独立“可选插件”模块，首次搭建用户可以明确跳过。
- 新增插件中心，统一展示插件版本、状态、适用问题、不适用问题和说明入口。
- BBR + FQ 继续保持独立可选，不被主程序自动安装、启用或维修。
- 仓库规则要求新插件必须登记，并提供独立版本、兼容、检查、恢复和卸载入口。
- 发布检查自动发现插件目录，阻止未登记或缺少说明的插件进入稳定版本。

## v1.7.4 - 2026-08-13

- 将“单一主路径、渐进展开”纳入项目宗旨和仓库强制规则。
- 第一次搭建教程从约300行旧式全流程改为约100行总路线，每一步只有一个推荐下一步。
- Mac和Windows教程删除重复的客户端、故障和BBR操作，首次成功后才展示迁移、升级等可选情况。
- 新增新手导航预算，阻止总路线和平台教程重新膨胀或复制专题命令。
- 发布检查验证路线篇幅、唯一客户端入口及二维码和插件命令隔离。

## v1.7.3 - 2026-08-13

- 新增独立 macOS 部署指南，GitHub 首页的 Mac 入口不再跳转到跨平台完整 SOP。
- 新增客户端导入与网络验收专题，统一维护 v2rayN、小火箭、比特浏览器、二维码和复制链接说明。
- 完成模块化审计；运行代码、诊断、安全恢复和 BBR 插件未发现职责越界或额外常驻负担。
- 模块索引和架构文档补充分平台教程、客户端教程的职责与修改路由。
- GitHub问题页增加按平台和客户端分类的教程入口。
- 发布检查新增 macOS、客户端专题、首页入口、版本标签和文档本地链接验证。

## v1.7.2 - 2026-08-13

- 新增根目录 `AGENTS.md`，把项目宗旨转为 Codex、自动化工具和人工贡献者都必须遵守的仓库级规则。
- 明确新手优先、未知即停止、白名单维修、单一常驻进程、插件优先、兼容、隐私和性能边界。
- 增加从教程到按需模块、插件、核心逻辑的决策顺序，防止简单问题被过度工程化。
- 发布检查强制验证规则文件、宗旨唯一来源、未知故障边界和常驻进程边界。

## v1.7.1 - 2026-08-13

- 重构 GitHub 首页，突出项目宗旨、稳定版本、自动检查、新手入口、模块地图和文档导航。
- 新增模块索引，明确每个文件的职责、运行方式、依赖边界、修改路由和验收要求。
- 新增中文故障反馈与功能建议表单，提交前引导脱敏、复现、模块归属和性能影响说明。
- 新增 Pull Request 模板，将安全、兼容、插件优先、测试和复杂度检查固化为清单。
- 修复 v1.7.0 两份文档末尾空行，并补上提交前未覆盖未跟踪文件的发布检查缺口。
- 发布检查现在直接扫描全部项目文本，阻止行尾空白和多余的文件末尾空行。

## v1.7.0 - 2026-08-13

- 正式确立“新手防呆、模块隔离、稳定轻量、步骤少、可记录复盘但不过度复杂”的项目宗旨。
- 新增核心模块边界和性能预算：正常运行仅保留 GOST 常驻，不引入面板、数据库或后台调度。
- 规定非必要能力插件优先，主程序不自动启用插件，扩展必须声明资源影响和卸载方法。
- 新增功能准入问题，优先用教程解决偶发需求，只把重复且原因明确的问题转成自动能力。
- 明确 `2.0` 只用于不兼容的架构或使用方式变化，功能增长本身不构成主版本升级。
- 发布检查新增宗旨、架构、复杂度和性能影响的一致性要求。

## v1.6.2 - 2026-08-13

- 新增 `socksctl guide` 中文编号菜单，减少新手复制错命令、粘贴说明文字或在错误位置执行的风险。
- 新增 `socks-doctor --no-record` 严格只读模式，不写事件档案。
- 外部检测固定为两个目标各两次、重试间隔 2 秒、单次超时 15 秒。
- systemd 限制为 60 秒内最多启动 5 次、每次间隔 5 秒，防止无限重启。
- 健康快照新增 SHA-256 清单；恢复前验证结构、凭据格式和内容完整性。
- 同一故障自动恢复一次后进入 30 分钟冷却，再次出现时熔断并转人工检查。
- 安装前检查磁盘空间和现有健康快照，阻止意外降级；明确降级需要交互确认。
- 新增 `socksctl reports` 和 `report-delete`，报告最多自动保留最近 5 份。

## v1.6.1 - 2026-08-12

- 将 `socksctl heal` 收紧为严格白名单：只有证据明确的配置故障允许恢复健康快照。
- 服务单独停止、未知错误、端口冲突等非白名单问题停止自动维修，保持配置不变并给出中文说明。
- 区分“端口无人监听”和“端口被其他程序占用”，避免对未知占用者执行错误维修。
- `socksctl restart` 失败时不再猜测原因或自动回退，只记录事件并指导人工检查。
- 新增 `socksctl report`，生成权限为 `0600` 的脱敏故障报告，便于完整提供给 Codex。
- 新增手动检查、维修与 Codex 求助教程。

## v1.6.0 - 2026-08-12

- 主程序增加全局运行锁、参数一致性判断和健康状态判断，安全跳过无意义的重复安装。
- 安装、升级和密码轮换改为事务式流程：修改前快照、修改后验收、失败后恢复。
- 新增 `socksctl doctor`，检查服务、端口归属、配置权限、重启次数、磁盘、时间、OOM、外部成功率和响应耗时。
- 新增 `socksctl incidents`，通过事件编号查询首次或重复故障、处理动作和恢复结果，默认不记录密码。
- 新增 `socksctl heal` 和 `socksctl recover`，分别用于受控自动恢复和确认后的手工恢复。
- 外部网络异常只记录不回退，避免检测站、VPN、安全组或运营商故障触发错误覆写。
- BBR 插件升级到 `1.1.0`，增加防重复、运行锁、配置冲突检测、`health` 与 `repair`。

## v1.5.0 - 2026-08-12

- 新增独立可选的 BBR + FQ 插件 `addons/bbr/`，不并入或自动调用 SOCKS5 主安装程序。
- 插件默认只安装 `bbrctl` 管理命令，必须由用户单独质检并确认启用。
- 首次启用保存原拥塞算法与队列规则，支持一键恢复；启用失败时尝试自动回退。
- 插件不增加端口，不修改 GOST、节点 IP、SOCKS5 凭据、安全组或旧服务迁移备份。
- 增加插件版本 `1.0.0`、主程序兼容声明、独立教程和随主版本运行的发布检查。

## v1.4.0 - 2026-08-12

- 新增安装前只读质检，识别 Ubuntu 镜像、CPU 架构、systemd、依赖、旧代理服务和端口占用。
- 新增受控覆写程序，仅迁移能够安全解析的旧 sing-box SOCKS5 账号密码。
- 覆写前自动备份旧配置；安装后进行代理出口验收，失败时停止新 GOST 并尝试恢复旧 sing-box。
- x-ui、Xray、v2ray 和未知进程保持“只报告、不自动停用”，避免误伤其他协议。
- 当前兼容范围收敛为 Ubuntu 20.04/22.04/24.04 的 amd64/arm64 镜像；其他系统留待实测后增加。

## v1.3.0 - 2026-08-12

- 新增 `socksctl export`，从服务器导出手工连接信息、v2rayN 链接和 Shadowrocket 链接。
- 新增 `socksctl qr`，在服务器终端本地生成客户端扫码二维码，不经过第三方二维码网站。
- `socksctl info` 新增服务器公网 IP 和服务运行状态，方便从 Mac 或 Windows 快速查询。
- 节点真实信息继续只保存在服务器，不使用本地节点清单，也不上传公开 GitHub。
- 安装配置新增公网 IP 字段，并兼容旧配置自动补全。

## v1.2.1 - 2026-08-11

- 新节点默认直接使用 VPS 公网 IP 作为节点名称。
- Mac 与 Windows/Xshell 安装命令不再要求额外填写 `--name`。
- 保留 `--name` 作为特殊情况下的兼容选项。
- 同步更新完整教程、Xshell 教程和版本检查。

## v1.2.0 - 2026-08-11

- 新增完整的跨平台搭建、验收、维护、升级和回退教程。
- 为每个稳定标签增加独立版本说明。
- 增加固定的版本维护规则和发布清单。
- 增加本地发布检查及 GitHub Actions 自动校验。
- 明确当前推荐工具版本与固定 GOST 核心版本。

## v1.1.0 - 2026-08-11

- 新增 Windows + Xshell 部署入口 `xshell-install.sh`。
- 新增固定版本下载、Xftp/SFTP 上传、安全组和日常维护指南。
- Xshell 入口自动补齐 Ubuntu/Debian 所需基础依赖。
- 保持原有 macOS `deploy.sh` 用法不变。

## v1.0.0 - 2026-08-11

- 首个稳定版本。
- 支持从 macOS 通过 SSH/SCP 部署单端口 GOST SOCKS5。
- 默认统一使用 `31080/TCP`。
- 提供安装、卸载、状态检查、日志和密码轮换工具。
- 固定并校验官方 GOST 3.2.6。
