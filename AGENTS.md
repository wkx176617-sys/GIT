# 仓库开发规则

本文件适用于整个仓库，供 Codex、其他自动化工具和人工贡献者在修改项目前读取。

## 项目宗旨

让第一次接触服务器的人，也能用尽量少的步骤安全搭建和维护 SOCKS5；让功能可以扩展，
但不让核心变成笨重的全权调度平台；让问题有记录、有回退、有复盘，项目因此持续变好。

`docs/project-principles.md` 是项目宗旨、性能预算和功能准入条件的唯一完整来源。规则与实现
发生冲突时，先停止修改并依据该文档收敛方案，不得通过增加复杂度绕过规则。

## 必须遵守

1. 新手优先：推荐流程只能有一个，提示使用中文，命令应可直接复制；危险操作必须确认。
2. 稳定优先：检测可以全面，自动维修只处理原因明确、可验证、可回退的白名单故障。
3. 未知即停止：保留现场、记录事件并说明人工检查步骤，不猜测、不强行覆写。
4. 核心轻量：正常运行只允许 GOST 作为业务常驻进程，不新增面板、数据库、定时扫描或管理端口。
5. 模块隔离：非核心功能优先放入 `addons/`，独立安装、卸载和版本管理，主程序不得自动启用；
   BBR + FQ 已经由重复真实故障验证为核心网络能力，必须保留显式开关、原设置和恢复入口。
6. 向后兼容：不得无迁移方案地改变已有命令、配置、凭据、快照或恢复流程。
7. 隐私安全：仓库、日志、测试和示例不得包含真实 IP、密码、Token、私钥、二维码或客户资料。
8. 性能克制：能够用教程解决的问题不增加代码；能够按需运行的功能不做后台常驻。
9. 单一主路径：新手页面只展示当前步骤和一个推荐下一步；平台、客户端、故障和插件各自只有
   一个权威教程，重复内容改为链接，可选分支必须放在主流程完成之后。返回、首页和搜索属于
   辅助导航，不得包装成新的推荐流程；统一由导航清单生成和校验。
10. 状态分流：不知道是否安装过时只进入智能安装/升级；确认本项目节点后才进入稳定版本切换。
    切换模块不得兼任首次安装或其他代理迁移；降版只恢复身份一致、校验通过的本机健康快照，
    并要求同一命令携带一次性明确授权参数。

## 修改流程

修改前：

- 先阅读 `docs/project-principles.md` 和 `docs/modules.md`，确认模块归属。
- 回答功能准入问题：是否真实重复出现、能否只改教程、能否做插件、失败能否回退。
- 检查工作区已有改动，不覆盖与当前任务无关的用户内容。

修改后：

- 同步 README、相关教程、`CHANGELOG.md`、版本说明和版本引用。
- 新增教程前先确认现有专题能否承载；相同命令不得在多个专题中分别维护。
- 新手教程必须登记到 `docs/navigator/navigation.tsv`，运行 `scripts/docs-navigation --write`，
  不得手工复制或修改生成的导航区块。
- 新增插件必须登记到 `addons/README.md`，并提供独立版本、兼容、检查、恢复和卸载入口。
- 修改 README 或公开导航时遵守 `docs/visual-style.md`，保持柔和留白且不隐藏安全信息。
- 版本说明必须列出常驻进程、端口、定时任务、依赖、用户步骤和模块耦合的变化。
- 运行 `bash scripts/release-check.sh`；检查失败不得提交或发布。
- 新增自动维修必须同时提供故障证据、白名单边界、有限重试、冷却、验收和回退测试。

## 决策顺序

遇到多种实现方式时，按以下顺序选择：

1. 不改代码也能安全解决；
2. 更新教程或提示；
3. 增加按需调用的独立模块；
4. 增加独立插件；
5. 最后才修改核心安装或运行逻辑。

任何方案一旦突破 `docs/project-principles.md` 的资源预算，必须在版本说明中解释必要性、关闭
方法和回退方法；无法证明必要时不实施。
