name: Publish tutorial navigator

on:
  push:
    branches: [main]
    paths:
      - 'docs/navigator/**'
      - '.github/workflows/docs-navigation.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload static navigator
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/navigator
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
